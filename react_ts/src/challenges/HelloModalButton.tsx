/*
Problem:
Write a React component that displays a modal dialog box with a message and a button. When the user clicks the button, the dialog box should be closed.

Example:
If the message is 'Hello, world!',
the component should display a dialog box with the message and a button.
When the user clicks the button, the dialog box should be closed.

Import your component into src/App.tsx and render it.
*/
